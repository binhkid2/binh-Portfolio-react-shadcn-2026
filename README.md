```
npx shadcn@latest add button sonner  tooltip accordion sheet skeleton toggle switch carousel aspect-ratio badge tabs slider select card alert avatar button dialog dropdown-menu input label progress separator textarea scroll-area popover table radio-group checkbox
```